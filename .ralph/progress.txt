PP10 electric HWS (RE-TEST as easy swap, standard) — FAIL. STILL -> $99 "3 line items failed grounding" even framed as simple like-for-like swap. Violates easy-job rule. Reliably broken (3 rounds).

================================================================================
RALPH ROUND-3 SUMMARY (20 tests): ~14 PASS / 2 borderline-PARTIAL / 4 FAIL
LABOUR FIX: PERFECT everywhere (EE9 downlight x4=1.6hr; no count-as-hours in any of 20). Materials grounded@catalogue+markup on every auto-quote.
EASY-JOB RULE (auto-quote not $99): SATISFIED for downlights, GPOs, smoke, fans, cooktops, outdoor lights, taps, washers, cistern repair, drains, CCTV — all auto-quote. GOOD.
FAILS (violate easy-job rule, all catalogue-gap grounding -> $99): PP9 toilet suite (3rd round), PP10 electric HWS (3rd round), EE10 diagnostic (should be $165 fee).
NEW BUG: EE7 customer-supplied fan billed $178 for the customer's own fan (picker/chosen-product ignores supply mode; assembly items like GPO/cooktop correctly install-only).
INCONSISTENT: PP7 gas connection $99 this round vs auto-quote round1 (non-deterministic grounding).
PICKER SUBSTITUTION: kitchen mixer->garden tap, dimmable->Black Fireflies, premium-DC->AC (standard/LLM path often corrects; recipe catalogue too thin per category).
MIS-DEFAULT UPSELL persists: repair requests (washer, cistern) default selected_tier to full-replacement tier.
ROOT FIX for the FAILS = catalogue the common sundries (tempering valve, pan connector, isolation valve) OR don't fail the WHOLE quote when only a minor sundry line is ungroundable (drop/soften it). Diagnostic needs its $165 flat-fee/0hr to satisfy min-labour.

================================================================================
RALPH RUN 2026-06-11 — Solar Estimate Premium (branch ralph/estimator-premium)
All 7 stories DONE. Gates: vitest 2609/2609 pass, next build pass, tsc at the
pre-existing 9-error baseline (admin-loader/agents/spec-guard/cron-kb-sync test
files, untouched), eslint clean on every touched file (877 project-wide errors
all pre-exist in dashboard/estimate/cesium).
Commits: 98ab3c5 S1 explainers, 932f4f8 S2 assumptions-view, 47ae142 S3 page,
7887aad S4 form, 8016870 S5 manual volumetric, 83cb53b S6 AI panels image.
LEARNINGS:
- manual-fallback synthesised ONE max-roof panel_config; sizing.nearestConfig
  gave every tier the full roof's DC energy -> CEC +/-35% blowups on real
  export limits. Fixed with a linear 1..max config ladder (S5).
- A literal file named 'nul' in quotemate-automation/ (Windows reserved name)
  makes Turbopack panic while Tailwind scans sources: 'Failed to write app
  endpoint ... reading file ...(nul)'. Remove it via the Win32 device-path
  prefix from cmd, AND clear .next (the panic poisons the persisted cache).
- .env.development.local points dev at Supabase avzrgqjzivtukydfzpof (NOT the
  prod bobvihqwhtcbxneelfns from .env.local). Migrations must be applied to
  BOTH; dev project had no 'intake-photos' bucket (created 2026-06-11).
- Migration numbering: 102 was already taken by plan_extraction_pricing
  (estimator work); solar panels preview became 103. Applied to prod + dev.
- Gemini panels-after render verified live end-to-end: ~25-28s, 1200px jpg,
  CAS retry from 'failed' worked after the bucket fix.

================================================================================
RALPH RUN 2026-06-12 — Commercial Painting Estimator (strategy v11, spec
docs/superpowers/specs/2026-06-12-commercial-painting-estimator-design.md)
All phases 0-7 DONE on local main. Gates: vitest 2771/2771, next build green,
tsc at the same pre-existing 9-error baseline, eslint clean on all new files.
IGA acceptance 17/17 + Gemini preview verified on the real pilot documents.
LEARNINGS:
- PS5.1 Get-Content/Set-Content round-trips mangle UTF-8 (mojibake) — never
  rewrite docs via the shell; use the Edit tool (strategy.md needed a
  git-checkout restore + re-apply).
- Hand-rolled structural Supabase client types hit TS2589 excessive-depth;
  type IO wrappers against the real SupabaseClient import instead.
- 'wash' as a low_sheen keyword captured 'limewash' — keyword maps need
  negative tests for adjacent vocabulary.
- The exhaustive tabLabel switch in app/dashboard/page.tsx breaks compile on
  any new Tab member — by design; remember it when adding tabs.
- Migration numbering: 107 as per spec (102=plan pricing, 103=solar panels,
  104-106 taken by parallel sessions). Applied to BOTH Supabase projects.
- Opus 4.8 over the 9.8MB 15-page IGA set: 63s, 19 surface lines, finishes
  schedule read correctly. Sonnet transcription of the painter's takeoff:
  37/34 lines, 420 m2 retail ceiling exact.
