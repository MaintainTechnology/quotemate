A big accent mono figure over a mono uppercase label — the brand's headline numbers.

```jsx
<Stat value="< 1 min" label="Per quote drafted" />
<Stat value="$0" label="Cut of your jobs" />
```

- Figure is JetBrains Mono, weight 700, accent, `tabular-nums`, fluid clamp size. Keep values concrete and defensible — no round-for-effect stats.
- Lay several out in a row divided by hairlines for a stat band.
